name: tccapp
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  # Dependências do Firebase
  firebase_core: ^3.12.1
  cloud_firestore: ^5.6.5 # Versão mais recente compatível com firebase_core ^2.0.0
  firebase_auth: ^5.5.1 # Adicionado para autenticação
  logger: ^1.1.0 # Adicionado para logging (substitui o uso de print)
  table_calendar: ^3.2.0
  # Dependências do Flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^5.0.0


flutter:


  uses-material-design: true


